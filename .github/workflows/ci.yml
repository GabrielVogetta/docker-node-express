name: Continuous Integration

on:
  push: 
    branches: [test]
  pull_request:
    branches: [test]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkando código
        uses: actions/checkout@v7

      - name: Setando Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Instalando dependências
        run: npm ci

      - name: Simulando testes
        run: npm run test